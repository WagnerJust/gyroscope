# Repo agent personas

Specialized agent personas scoped to this repo. `AGENTS.md` routes here.

**Status: none defined yet.** gyroscope's own design explicitly defers
agency-persona wiring (see `TODO.md` → "Later"), so this repo has no repo-specific
personas at the moment. This directory exists as the blessed home for them.

When personas are added, keep one markdown file per persona (e.g.
`enforcement-adapter-author.md`, `standard-writer-reviewer.md`), each customized
to this repo — adapted from references such as agency-agents, not installed
wholesale.

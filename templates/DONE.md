# DONE

> Completed-work archive for this repo. Move a task's line here from `TODO.md`
> once it's finished, newest on top. **This file is NOT injected** by the
> SessionStart hook — it is history, kept out of the per-session context so
> `TODO.md` stays a small "what's open" view. Tracked in git; append-only in
> spirit (edit for accuracy, don't prune the trail).

**Legend:** `[x]` done

## Completed
- [x] Move finished `[x]` items here from `TODO.md` so the open list stays lean.

# local.md — your local notes

Personal, machine-specific context for **this** developer. Gitignored — never
committed, never shared. Each developer keeps their own — fill in the parts that
apply to you and delete the rest.

Agents: read this for how *this* developer's environment differs from the repo
defaults. **Do not paste secrets here — reference where they live instead.**

## Stack & tooling
- Replace with your machine's quirks — e.g. package manager is pnpm not npm;
  runtime / version manager, editor, shell.

## Local services
- Replace with the ports and services you run locally — e.g. Postgres on :5433,
  Redis on :6380.

## Personal workflow
- Replace with your branch / PR habits, scratch commands, anything the agent
  should respect.

## Secrets — location only
- Replace with *where* your secrets live, never the values — e.g. "API keys in
  ~/.config/acme/.env — do not print them".

<!-- gyroscope creates this and adds `.local/` to the repo's .gitignore. Default
on; override in the gyroscope config. -->

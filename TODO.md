# gyroscope — build TODO

> Living build tracker, hand-maintained (not via the todo tool). **Open work
> only** — this file is injected by the SessionStart hook every session, so keep
> it small. When a task is done, move its line to `DONE.md` (the routed, not-
> injected archive). Design docs & scratch live in `.local/` (gitignored).

**Legend:** `[ ]` not started · `[~] `in progress · `[x]` done (then move to `DONE.md`)

## Next
- [ ] **Optional "sync agents" script so non-gyroscope devs get personas + the hook installed per session — without committing `.claude/` or overriding same-named agents.** The ships-to-team gap has two fixes: commit `.claude/` (see the warning we already ship), or reconstruct it per-dev. This is the second: gyroscope generates a committed, dependency-free script (bash — the hook is already bash) that copies each `docs/agents/<file>.md` → `.claude/agents/<name>.md` and ensures the SessionStart hook, run at session start so a teammate who never installed gyroscope still gets dispatchable subagents + hub injection. **Design:** (1) invoke via the SessionStart hook (deterministic), not a route (agent-executed, unreliable) — but note this shifts the pure-`cat` hook to one that *writes*; it writes only gitignored, gyroscope-owned `.claude/agents/`, never user content. (2) **Avoid dual-implementation drift** — don't re-parse YAML frontmatter in bash; gyroscope bakes the explicit `src → dest` copy-list into the generated script (like the mirror is generated), regenerated on `init`/`check --fix`. (3) **Non-override:** skip-if-exists — never overwrite an existing `.claude/agents/<name>.md` (a dev's own agent, or a `~/.claude/agents/` user-scope one); trade-off: won't refresh a stale mirror unless the file carries a gyroscope-owned marker the script may update. (4) Cross-platform: bash-only (Windows gap; the hook already assumes a shell). (5) Trust surface: a committed script auto-run via a hook — Claude hooks are `/trust`-gated; keep it minimal + readable. Gate on a `sync`/`syncScript` toggle. Ties to the `.claude/`-gitignored warning already shipped.
- [ ] **Optional `codeReview` toggle — scaffold a curated reviewer-persona + review-process bundle (repeated setup today).** Justin re-installs the same review suite per repo (grounded in `~/Work/lajolla`: `docs/agents/{frontend,security,design,backend,ux,cicd,db,experience}-reviewer.md` + a `code-review` orchestrator + `docs/adr/0002-ground-review-against-intent.md` + a review-checkpoints pattern). It is the persona mechanism as a canonical opinionated preset. **Design:** the binary never authors persona content (binary/skill split), so this is a **skill** feature + a `gyroscope.json` flag — when `codeReview` (or a persona *preset*) is set, the `/gyroscope` skill scaffolds the review bundle (reviewer personas customized per repo like normal personas, a review-process spoke / review-checkpoints, optionally the grounding ADR) instead of the from-scratch interview. **Pushback/scope:** decide new toggle vs a "preset" within the persona flow; capture lajolla's exact suite as the canonical set; the bundle spans genres (personas + a process spoke + an ADR), broader than one persona. Complements Claude's built-in `/code-review` (that is the runner; this is the repo-specific reviewers + process).
- [ ] **Enforce general ADR usage + upkeep — directive (write them) + check (keep them healthy).** gyroscope scaffolds `docs/adr/` (TEMPLATE + 0001) and routes to it, but never enforces or maintains it. Add both halves, mirroring the personas/attribution pattern: **(usage — prompt-injection)** a managed hub directive instructing agents to record significant architectural decisions as ADRs in `docs/adr/` using the template, and reference them from code/docs — echoing the template's own "write an ADR only when the decision is hard to reverse, surprising, and a real trade-off" guard so it *encourages*, not over-produces. **(upkeep — check)** verify ADR hygiene: sequential numbering (no gaps/dupes), TEMPLATE present, each ADR carries a `Status` + `Date`, and no dangling reference to a nonexistent ADR number (the exact class of bug behind the shipped `docs/adr/0010` dead link — an upkeep check would have caught it). **Pushback/scope:** "enforce" stays soft — ADR discipline is judgment (not every change needs one), so the directive encourages and the check polices *structure* (numbering/template/status/referenced-exists), never "did you write enough ADRs" (unknowable). Gate behind the existing `adr` spoke (or a new `enforce.adr`); structural findings as notes vs drift — TBD.
- [ ] **Broaden harness coverage — the AGENTS.md-native cohort (Codex, OpenCode, …) + verify "OpenClaude".** gyroscope's hub *is* `AGENTS.md`, the emerging cross-tool convention several newer harnesses read **natively** — OpenAI **Codex** CLI and **OpenCode** both load `AGENTS.md` (root + nested) with no pointer file needed. gyroscope has no target/adapter/test/acknowledgment for them, so a user can't tell they're already first-class. Investigate + register per tool: (1) **Codex** — reads `AGENTS.md` natively; likely needs no pointer and no force-inject (it reads the hub by default) — mostly *document* it as a first-class consumer + verify nested-dir precedence and whether `~/.codex/`/project config wants anything. (2) **OpenCode** — same native `AGENTS.md` read; confirm, document, and check whether its config (`opencode.json`) wants a pointer or a session hook. (3) **"OpenClaude"** — **unverified**; confirm it is a real, distinct harness before scoping (may be a mix-up with OpenCode or a Claude-compatible fork). **Framing:** most need *nothing new* — they consume the hub directly, so the work is verification + a one-line acknowledgment (a `target` entry, or a CONTEXT/CONTRIBUTING note that `AGENTS.md` is their native format), NOT a pointer/adapter unless the tool has a real force-inject mechanism worth wiring (parallel to the Cursor/Zed enforcement-adapter items above). Lean into the strength: gyroscope's hub already speaks the standard these tools read. (Existing per-tool items: Cursor active adapter, Zed adapter, PI persona parity — separate.)

## Known non-issues (kept for context; not action items)
- [ ] (note, not fixed) `go build ./cmd/gyroscope` reports `dev (commit none, built unknown)` — no ldflags; `make build` is the versioned path. Expected, documented as the "quick local binary."

## Later — deferred (explicitly out of MVP)
- [ ] **PI persona sub-agents (extension-built) — parity for the `.claude/agents/` mirror.** PI has NO native sub-agent registry (by design — its README: "No sub-agents… build your own with extensions"), so there is nothing to mirror `docs/agents/` into the way F1–F4 do for Claude. Sub-agents are *buildable* via PI's extension API, though. To give PI parity, extend the gyroscope PI extension (`.pi/extensions/gyroscope-context.ts`) to register a sub-agent tool that, per `docs/agents/` persona, spawns `pi -p <prompt> --tools <persona tools>` with the persona as the system prompt. This is BUILDING runtime behavior (a harness feature), not a byte-copy mirror — larger scope than the Claude mirror, opt-in, gated on `enforce.pi` + `personas == on`. Keep `docs/agents/` canonical; the extension reads it live. See ADR 0010 (Claude mirror) for the docs-vs-registered rationale this would extend to PI.
- [ ] **Zed enforcement adapter** — active `enforce.Adapter` for Zed (the passive `.rules` doc-target pointer already exists; this is the force-inject side, parallel to Claude's SessionStart hook / PI's `session_start` extension). Investigate Zed's injection mechanism (does its agent support a session-start hook / rule that force-reads the hub, or is `.rules` native-read only?). If native-read only, there may be nothing to enforce — document that outcome. Wire behind the `enforce` config section (`zed`, opt-in) if a mechanism exists.
- [ ] **Cursor enforcement adapter** — active `enforce.Adapter` for Cursor (passive `.cursorrules` pointer already exists). Investigate Cursor's mechanism: `.cursor/rules/*.mdc` with `alwaysApply: true` (always-injected project rules) and/or Cursor hooks. An always-applied rule that force-reads the hub would be the enforcement analog. Wire behind the `enforce` config section (`cursor`, opt-in).
- [ ] **Terse output for gyroscope repos (toggleable) — deferred; caveman manually for now.** Brainstormed 2026-07-10; full option analysis in `.local/design/2026-07-10-terse-output-options.md`. Shortlist: recommend **Option 5, adapter-shaped hybrid** — canonical `docs/terse.md` (hub-routed) that each enforce adapter registers natively, mirroring the persona pattern (ADR 0010): Claude adapter writes `.claude/output-styles/terse.md` (native output-style; `keep-coding-instructions: true` **required** or it strips coding discipline) + `outputStyle` in the settings.json it already merges; PI adapter injects the doc; pointer harnesses doc-routed. Gated on a `terse` toggle in `gyroscope.json` (default off; on for gyroscope = dogfood). Content: original wording informed by caveman (MIT — reuse allowed with attribution, but plugin-shaped; keep code/commands/**error strings** byte-exact, never terse-ify security/irreversible/ambiguous). Open decisions in the design note: scope 5a (Claude-first) vs 5b (full); toggle semantics — (A) sticky opt-out via output-style vs (B) ephemeral per-session, which needs a launch-gated cat-hook instead (output-styles can't do ephemeral). Core tradeoff: output-style = strong persistence + zero added input tokens but sticky-only toggle; cat-hook = weaker persistence + input cost but true per-session gating.
